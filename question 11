# Question 11 calculate total bill after discount
total = float(input("enter total value"))
dis = float(input("enter discount"))
if total >=200 :
 print("Final Price:", total - (total * dis / 100))
else:
  print("Final Price:", total, "(no discount)")
